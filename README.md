# Valentine Page

## GitHub Pages (quick setup)
1. Create a new GitHub repo.
2. Add these files and push to the `main` branch.
3. In the repo **Settings → Pages**, choose **Deploy from a branch**, select **main** and **/ (root)**, then **Save**.
4. Share the URL shown in the Pages section.

Example link (with name):
`https://YOUR-USERNAME.github.io/YOUR-REPO/?name=Alex`
